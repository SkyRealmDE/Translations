skyrealm:

  general:
    transfer: "You will be transferred to another instance..."

  lobby:
    join: "%s joined."
    quit: "%s quit."

  commands:
    helloworld: "Hello world, %s!"

  cloud:
    no_servers: "No servers found."
    available_servers: "Currently available servers: <green><bold>%s"